# Need a local data directory to create and store a mongodb.
# If it is desired to store the database elsewhere locally change the path in 'start_mongo.py'
# If connecting to a remote db, change the mongoclient initialization as necessary.
